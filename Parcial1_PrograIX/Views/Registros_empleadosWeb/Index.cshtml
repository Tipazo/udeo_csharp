@model IEnumerable<Parcial1_PrograIX.Models.Registros_empleados>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.hora_registro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estudiante.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.hora_registro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estudiante.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
           
            @Html.ActionLink("Detalle del empleado", "Details", new {  id=item.estudiante_id }) 
      
        </td>
    </tr>
}

</table>
